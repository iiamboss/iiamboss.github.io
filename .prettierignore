# Ignore everything
/*

# Except these files & folders
!/src
!/public
!/.github
!tsconfig.json
!astro.config.ts
!astro-paper.config.ts
!package.json
!.prettierrc
!eslint.config.js
!README.md

# Ignore MDX files
*.mdx

# 글 본문은 포매터에서 뺀다. 본문 코드블록은 설명하려고 옮겨 적은 것이라
# 따옴표·줄바꿈·후행 쉼표가 원본 그대로여야 한다. prettier 를 걸면 그걸
# 저장소 스타일로 고쳐써서 글과 코드가 어긋난다.
/src/content